apiVersion: v1
kind: Service
metadata:
  name: flask-app-svc
spec:
  # NodePort позволяет нам получить доступ к приложению снаружи виртуалки
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
  selector:
    app: flask-app
